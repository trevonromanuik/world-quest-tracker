# import Faction from './faction.graphql'
# import Zone from './zone.graphql'

type Quest {
    _id: Int! @unique
    name: String!
    lastSeen: DateTime!
    type: Int!
    side: Int!
    factions: [Faction!]!
    zones: [Zone!]!
}